/*
    ATTENTION: The order matters!
    While runtime it works like a list so you can call Weeky[new Date().getDay] to get the weekday
*/

export enum Weekday {
    Sunday,
    Monday,
    Tuesday,
    Wednesday,
    Thursday,
    Friday,
    Saturday
}